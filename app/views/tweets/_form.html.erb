<%= form_with(model: tweet, local: true, :class => "w-100 mb-3") do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group my-5">
    <strong >Hello! How is your animalistic day going?</strong><br>
    
    <%= form.text_area :content, :class => "form-control mt-5 " %>
  </div>

  <div class="actions float-right">
    <%= form.submit "Twittear", :class => "btn btn-info p-2 " %>
  </div>
<% end %>
